document.addEventListener("DOMContentLoaded", () => {

    const waxButton = document.getElementById("waxButton");

    if (waxButton) {

        waxButton.addEventListener("click", () => {

            alert("RA BUTTON WORKS!");

        });

    }

});
